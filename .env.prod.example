## PRODUCTION ENVIRONMENT
# Copy this file to .env.prod to use it for production environment

## Database
FUSEKI_PASSWORD=admin # TODO: Change to strong password

## Middleware
MIDDLEWARE_VERSION= # Docker image tag. If empty, it takes latest
SEMAPPS_SPARQL_ENDPOINT=http://fuseki:3030/
SEMAPPS_HOME_URL=https://data.compiegnois.transiscope.org/
SEMAPPS_FRONT_URL=https://compiegnois.transiscope.org/
SEMAPPS_OIDC_ISSUER=https://login.lescommuns.org/auth/realms/master/
SEMAPPS_OIDC_CLIENT_ID=semapps
SEMAPPS_REDIS_CACHE_URL=redis://redis:6379/0
SEMAPPS_RESOURCESPERMISSIONS_ANYUSER_WRITE=true
SEMAPPS_SUPER_ADMINS=

## Frontend
FRONTEND_VERSION= # Docker image tag. If empty, it takes latest
VITE_MIDDLEWARE_URL=https://data.compiegnois.transiscope.org/
VITE_MAPBOX_ACCESS_TOKEN=MyMapBoxToken # TODO: Change to real Mapbox token
